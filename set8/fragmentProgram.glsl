uniform float t;
varying float u, v;

uniform sampler2D leaf, sky;

void main() {
  /*
  //u += sin(t);
  //v += t;
  gl_FragColor = vec4(sin(40*u),sin(40*v),0.0,1.0);
  */


  /* If per-vertex normal mode is off, calculate normal  */
  /* Use sphere/environment mapping to determine fragment color  */
  /* Get leaf color using texture coordinate  */
  /* Blend two textures together using leaf alpha  */





}
